<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ayush.loginregister">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <!--<uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />-->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="Register" >


        </activity>
        <activity
            android:name=".HomeScreenActivity"
            android:label="Home" />
        <activity
            android:name=".WalletActivity"
            android:label="Wallet" />
        <activity
            android:name=".ExchangeCurrencyActivity"
            android:label="@string/title_exchange_currency"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Nav_WalletActivity"
            android:label="@string/title_activity_nav__wallet"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".New_LoginActivity"
            android:label="@string/title_activity_new__login"></activity>
    </application>

</manifest>
